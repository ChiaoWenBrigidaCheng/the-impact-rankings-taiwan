# THE 大學影響力排名（台灣）

GitHub Pages 穩定版：網站前台讀取 `data.json`，不再直接解析 Excel，避免圖表空白、null 或資料缺漏。

## 上傳方式

將以下兩個檔案放在 GitHub repository 根目錄：

- `index.html`
- `data.json`

## 更新資料

未來如有新版 Excel，請先將 Excel 轉成同格式 `data.json`，再覆蓋 GitHub 上的 `data.json`。
