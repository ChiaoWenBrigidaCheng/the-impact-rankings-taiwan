# 台灣大專校院 THE Impact Rankings 追蹤平台

2019-2026區間。GitHub Pages 穩定版，網站前台讀取 `data.json`。

## 上傳方式
將 `index.html` 與 `data.json` 放在 GitHub repository 根目錄。

## 更新資料
未來如有新版 Excel，請先轉成同格式 `data.json`，再覆蓋 GitHub 上的 `data.json`。
