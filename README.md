# 台灣大專校院 THE Impact Rankings 追蹤平台

2019-2026區間。GitHub Pages 版本，網站前台直接讀取 `data.xlsm`。

## 上傳方式
將 `index.html` 與 `data.xlsm` 放在 GitHub repository 根目錄。

## 更新資料
未來如有新版 Excel，只要將檔名改為 `data.xlsm`，並在 GitHub repository 根目錄覆蓋舊的 `data.xlsm` 即可。
